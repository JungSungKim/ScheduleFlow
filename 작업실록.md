# ScheduleFlow — 작업실록

날짜별 작업 이력. 최신 항목이 위에 온다.

---

## 2026-06-30

- feat: TODO·출장 완료 항목 월별 그룹 접기 — `<details>/<summary>` 네이티브 요소로 구현
- refactor: `renderTodos` → `_sortTodoList` / `_renderTodoItem` / `_renderDoneByMonth` 분리
- refactor: `renderTrips` → `_renderTripCard` / `_renderCompletedTripsByMonth` 분리
- feat: `fmtYearMonth()` 헬퍼 추가 (app.js)
- style: 월별 그룹 헤더·구분선 CSS 추가 (index.css v19)
- chore: SW CACHE_VER sf-v25, 버전 번호 일괄 증가 후 Firebase 배포

---

## 2026-06-09

- feat: 리얼타임 크로스-디바이스 동기화 — Firestore onSnapshot으로 다른 기기 변경 즉시 반영
- feat: 헤더 수동 새로고침(↺) 버튼 + 동기화 토스트 알림
- docs: CLAUDE.md 오류 수정 — API명 3건 정정, 리얼타임 동기화 패턴 추가

---

## 2026-06-03 (추가)

- docs: Phase 4 로드맵 정리 — AI 문서 초안, 자연어 입력 파싱 제거 (실용성 낮음)

## 2026-06-03

- fix: iOS PWA Google 로그인 "액세스 차단됨" — Google Cloud Console에서 `web.app/__/auth/handler` redirect URI 수동 등록으로 해결
- fix: 모바일 헤더 "대시보드" 타이틀 2줄 표기 — `.page-title` 1rem + nowrap + ellipsis, Ctrl+K 텍스트 모바일 숨김
- fix: TODO 페이지 가로 폭 초과로 배율 변경 — `.todo-meta`에 `flex-wrap: wrap` 추가
- chore: `.claude/settings.local.json` git 추적 시작 (다른 PC pull 시 설정 유지)
- docs: CLAUDE.md 개선 — sw.js/manifest/icon.svg 구조 추가, PWA·SW·이중바인딩 gotchas, Store API, Firebase CLI 토큰 경로

## 2026-06-02 (추가2)

- TODO → 캘린더 드래그로 날짜 변경 (calendar.js calDayDrop + drop-target CSS)
- 알림/리마인더: 브라우저 Notification API, 마감 30분 전, settings 패널 토글
- 문서 템플릿 기본값: Store.getDocTemplate/saveDocTemplate, 사전신청서·보고서 "기본값 저장" 버튼

## 2026-06-02 (추가)

- PWA 설치 지원: manifest.json, icon.svg, sw.js 생성; index.html 메타태그 추가; app.js v16 설치 프롬프트 버튼

## 2026-06-02

- 프로젝트 폴더 구조 재정비: 소스 → `public/`, 문서 체계 수립
- CLAUDE.md / ROADMAP.md / DECISIONS.md / DEV_LOG.md 초안 작성
- 전역 세션 커맨드 등록 (`scheduleflow-start`, `scheduleflow-exit`)
- Direction B Phase 1: neutral 팔레트, 2-column 사이드바(rail+panel), Lucide 아이콘
- 버그 #1~#9 수정 (todoAppearsOn, UTC, esc중복, DnD, deleteTrip, popup, isTrip, now클로저, 날짜null)
- feat: 전역 검색(Ctrl+K), 데이터 내보내기/가져오기(JSON), 반복 일정 캘린더 렌더링

## 2026-04-07

- 대시보드 레이아웃 개편 및 반응형 적용
- 대시보드 카드 Drag & Drop 순서 저장 (SortableJS 적용)
- 대시보드 미니 캘린더 업그레이드 (바 스택 알고리즘 동기화)
- 단축 홈 버튼 추가

## 2026-04-01

- 초기 MVP 세션 요약 (docs/sessions/session_summary_2026_04_01.md 참조)
